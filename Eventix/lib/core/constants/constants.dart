import 'package:flutter/material.dart';

class Constants {
  static String appName = 'Fast';
  static String currency = '\SAR';
  static Locale engLocal = Locale('en');
  static Locale arLocal = Locale('ar');
}
